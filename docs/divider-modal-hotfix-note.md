# PDF 간지 모달 레이아웃 핫픽스

- `divider-studio`가 간지 모달 DOM 구조를 만든 뒤에만 좁은 왼쪽 패널 레이아웃을 적용합니다.
- 이전 구현처럼 원본 모달의 직접 자식들을 먼저 이동하지 않습니다.
- 간지 이미지 패널과 취소/삽입 버튼만 기존 `divider-studio-controls` 내부로 이동합니다.
- 간지 모달을 실제로 클릭해 열고 닫는 브라우저 회귀 테스트를 PDF 프로그램 셸 품질 게이트에 추가했습니다.
