# 통합 디자인 편집기 · 표지 통합 기준

## 목표

표지디자인, 포스터, 전단, 2단·3단 리플렛, 사용자 지정 문서를 하나의 일반 디자인 편집 엔진에서 편집한다.
기존 표지 기능을 삭제하거나 단순화하는 것이 아니라, 공통 기능은 통합 편집기 기능으로 옮기고 책등/제본처럼 표지에만 필요한 기능은 cover capability로 유지한다.

## 통합 표지 문서 모델

표지는 한 개의 펼침면(surface)으로 취급한다.

- 순서: `뒤표지 | 책등 | 앞표지`
- 완성 페이지 규격 기본값: A4 210 × 297mm
- 도련 기본값: 3mm
- 안전여백 기본값: 10mm
- 본문 페이지 수 기본값: 160
- 종이 두께 기본값: 0.1mm
- 제본 보정 기본값: 0.5mm
- 기본 책등: `ceil(160 / 2) × 0.1 + 0.5 = 8.5mm`
- 기본 펼침면 폭(도련 제외): `210 + 8.5 + 210 = 428.5mm`
- 기본 출력 전체 크기: `434.5 × 303mm`

통합 편집기 프로젝트에서는 `project.width`가 도련을 제외한 전체 펼침면 폭이고, 접지/경계선은 `trimWidth`, `trimWidth + spine` 위치에 둔다.

## 공통 편집기로 이전할 기능

- 글자 생성/편집/선택/이동/크기/회전
- 이미지 업로드/배치/크기/인쇄 해상도 확인
- 레이어, 잠금, 정렬, 실행취소/다시실행
- 프로젝트 저장/불러오기, 자동저장, 복구, 클라우드 프로젝트
- 출력 전 검사
- 300DPI PNG/PDF 출력 및 저장 직전 후검증

기존 표지 구현은 기능이 통합 편집기에 대응되는 것이 확인된 뒤에만 제거한다.

## 표지 전용으로 유지할 기능

- 본문 페이지 수/종이 두께/제본 보정에 따른 책등 자동 계산
- 책등 직접 입력
- 앞표지·책등·뒤표지 경계 및 전용 안전영역
- 책등 글자 방향(아래→위, 위→아래, 한 글자씩 세로)
- 좁은 책등의 글자 크기/안전성 검사
- 표지 전용 템플릿과 템플릿 안전성
- 전체 펼침면 전용 미리보기/가이드 표현

## 호환 원칙

1. 기존 `/perfect-binding-cover/`는 통합 완료 전까지 삭제하거나 기능 축소하지 않는다.
2. 기존 표지 프로젝트/템플릿을 읽을 수 있는 변환 경로를 만든 뒤 기본 진입점을 통합 편집기로 바꾼다.
3. 표지 출력은 현재와 동일하게 RGB 300DPI 래스터 출력을 기준으로 한다. CMYK/PDF-X/vector 지원으로 표시하지 않는다.
4. 표지 모드에서도 일반 디자인 편집기의 인쇄 후검증을 우회하지 않는다.
5. 책등 폭 변경 시 기존 글자/이미지/도형을 삭제하지 않고 펼침면 geometry만 갱신한다.

## 단계별 이전 순서

- Stage 41: 표지 펼침면 모델 + 기존 기능 보존 계약
- Stage 42: 통합 편집기 runtime에 cover model 연결 + 표지 모드 일반 편집기 진입
- Stage 43: 표지 규격/책등 설정 UI와 펼침면 geometry 실시간 갱신
- Stage 44: 책등 글자 방향/안전영역 이관
- Stage 45: 기존 표지 텍스트·이미지·레이어 기능 공통 엔진 대응 확인
- Stage 46: 기존 표지 프로젝트/자동저장 변환 및 호환
- Stage 47: 기존 표지 템플릿 호환
- Stage 48: 표지 300DPI 출력/가이드 PDF 통합
- Stage 49: 대응 완료된 중복 legacy 코드만 단계적으로 제거
- Stage 50: 표지·포스터·전단·리플렛 전체 브라우저 E2E 회귀 검증
